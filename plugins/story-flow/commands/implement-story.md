---
argument-hint: [@path/to/story.md]
description: Implememnt a story according to the requirements and tasks
---

Load BOTH skills in this order using the Skill tool:
1. First: `story-flow:implement-story-markdown` (plugin - general protocol)
2. Then: `implement-story-markdown` (project-level - overrides/extends the plugin)
3. Confirm both skills are loaded before continuing with the implementation.

Execute tasks according to $ARGUMENTS using subagents. Use context7.
